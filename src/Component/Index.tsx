import React from "react";
import { LoginForm } from "./LoginForm";

export const Index = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
      }}
    >
      <LoginForm />
    </div>
  );
};
